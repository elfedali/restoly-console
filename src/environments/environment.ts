export const environment = {
    production: false,
    API_URL: '',
    httpOptions: {
        headers: {
            'Content-Type': 'application/json'
        }
    },
    jsonApiHttpOptions: {
        headers: {
            'Content-Type': 'application/vnd.api+json'
        }
    }
}